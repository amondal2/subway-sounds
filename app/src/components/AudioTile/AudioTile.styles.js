import styled from 'styled-components';

const TileContainer = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
`;

const Icon = styled.img`
  height: 15px;
  width: 15px;
`;

export { TileContainer, Icon };
